stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `in` here, but I got `.` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `.` here, but I got `IO` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                               ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `identifier` here, but I got `.` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `!` here, but I got `println` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                                  ^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `=>` here, but I got `->` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                                             ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I was not expecting `)` here.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                                                        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I was not expecting `}` here.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) }
                                                                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `)` here, but I got `` instead.
   3 | 
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/typechecking/handle-expression/src/Main.camp

I expected `}` here, but I got `` instead.
   3 | 
       ^
compilation failed with 9 error(s)
"""
stderr = ""
exit = 1
