stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/tag-unions/tag-match-branches-exhaustive/src/Main.camp

I expected `=` here, but I got `|` instead.
   1 | x : Ok(I64) | Error(I64) = Ok(1)
                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/tag-unions/tag-match-branches-exhaustive/src/Main.camp

I expected `=` here, but I got `Ok` instead.
   1 | x : Ok(I64) | Error(I64) = Ok(1)
                                  ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/tag-unions/tag-match-branches-exhaustive/src/Main.camp

I expected `=>` here, but I got `->` instead.
   2 | y = match x { Ok(v) -> v | Error(e) -> e }
                           ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/tag-unions/tag-match-branches-exhaustive/src/Main.camp

I expected `=>` here, but I got `->` instead.
   2 | y = match x { Ok(v) -> v | Error(e) -> e }
                                           ^^
compilation failed with 4 error(s)
"""
stderr = ""
exit = 1
