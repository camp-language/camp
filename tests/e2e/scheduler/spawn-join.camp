main! = || ->{Spawn!} I64 {
  handle Spawn! in {
    h = Spawn!.spawn!(|| -> I64 { 42 })
    Spawn!.join!(h)
  } with {
    .spawn!(resume, f) => resume(0),
    .join!(resume, h_ref) => resume(0)
  }
}
