main! = || -[Spawn!]-> I64 {
  handle Spawn! in {
    $h: Handle = Spawn!.spawn!(|| -> I64 { 42 })
    Spawn!.join!(h)
  } with {
    .spawn!(resume, _f) => resume(0),
    .join!(resume, _h_ref) => resume(0)
  }
}
