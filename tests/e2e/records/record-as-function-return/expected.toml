stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `uppercase identifier` here, but I got `x` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                     ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `uppercase identifier` here, but I got `:` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `identifier` here, but I got `{` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                                ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `:` here, but I got `x` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                                  ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I was expecting a type here, but I found `:` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `:` here, but I got `}` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                                       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I was expecting a type here, but I found `}` instead.
   1 | mk = |a| -> { x: I64 } { { x: a } }
                                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `:` here, but I got `=` instead.
   2 | main! = || -> I64 { mk(42).x }
             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `identifier` here, but I got `{` instead.
   2 | main! = || -> I64 { mk(42).x }
                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `:` here, but I got `mk` instead.
   2 | main! = || -> I64 { mk(42).x }
                           ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I was expecting a type here, but I found `42` instead.
   2 | main! = || -> I64 { mk(42).x }
                              ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `identifier` here, but I got `.` instead.
   2 | main! = || -> I64 { mk(42).x }
                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `:` here, but I got `x` instead.
   2 | main! = || -> I64 { mk(42).x }
                                  ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I was expecting a type here, but I found `}` instead.
   2 | main! = || -> I64 { mk(42).x }
                                    ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I expected `}` here, but I got `` instead.
   3 | 
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/records/record-as-function-return/src/Main.camp

I was not expecting `` here.
   3 | 
       ^
compilation failed with 16 error(s)
"""
stderr = ""
exit = 1
