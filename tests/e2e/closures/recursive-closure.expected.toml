stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I was not expecting `)` here.
   1 | f! = () -> I64 { f!() }
             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I expected `)` here, but I got `->` instead.
   1 | f! = () -> I64 { f!() }
               ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I expected `=` here, but I got `{` instead.
   1 | f! = () -> I64 { f!() }
                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I expected `=` here, but I got `(` instead.
   1 | f! = () -> I64 { f!() }
                          ^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I was not expecting `)` here.
   1 | f! = () -> I64 { f!() }
                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I expected `=` here, but I got `` instead.
   2 | 
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/closures/recursive-closure/recursive-closure.camp

I was not expecting `` here.
   2 | 
       ^
compilation failed with 7 error(s)
"""
stderr = ""
exit = 1
