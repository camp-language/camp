stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/execution/recursive-call/recursive-call.camp

I expected `=` here, but I got `I64` instead.
   1 | loop! = (n) -> I64 { if n == 0 0 else loop!(n - 1) }
                      ^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/execution/recursive-call/recursive-call.camp

I was not expecting `!` here.
   1 | loop! = (n) -> I64 { if n == 0 0 else loop!(n - 1) }
                                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/execution/recursive-call/recursive-call.camp

I was not expecting `!` here.
   2 | main! = || -> I64 { loop!(5) }
                               ^
compilation failed with 3 error(s)
"""
stderr = ""
exit = 1
