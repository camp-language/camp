stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `}` instead.
   1 | effect Ask { read }
                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `handle` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                ^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `read` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                           ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `{` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                                       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `read` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                                             ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I expected `:` here, but I got `42` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                                                              ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-perform-return-value/effect-perform-return-value.camp

I was expecting a type here, but I found `)` instead.
   2 | result = handle Ask.read() with { Ask.read() -> resume(42) }
                                                                ^
compilation failed with 7 error(s)
"""
stderr = ""
exit = 1
