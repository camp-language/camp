stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                            ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `:` here, but I got `[` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `:` here, but I got `->` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                  ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                                     ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `:` here, but I got `[` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                                          ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-operations/src/Main.camp

I expected `:` here, but I got `->` instead.
   1 | IO! : { println!: || -[IO!]-> {}, readln!: || -[IO!]-> Str }
                                                           ^^
compilation failed with 8 error(s)
"""
stderr = ""
exit = 1
