Ask! : { read!: || -[Ask!]-> I64 }
_x = handle Ask! in Ask!.read!() with { .read!(resume) => resume(42) }

State! : { get!: || -[State!]-> I64 }
_y = handle Ask! in
      handle State! in
        Ask!.read!()
      with { .get!(resume) => resume(0) }
    with { .read!(resume) => resume(42) }
