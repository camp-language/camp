stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   1 | IO! : { println!: || -[IO!]-> {} }
                            ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `[` instead.
   1 | IO! : { println!: || -[IO!]-> {} }
                             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   1 | IO! : { println!: || -[IO!]-> {} }
                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `->` instead.
   1 | IO! : { println!: || -[IO!]-> {} }
                                  ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `[` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                            ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `->` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                    ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                                     ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `[` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                                             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-multiple-effects/src/Main.camp

I expected `:` here, but I got `->` instead.
   2 | State! : { get!: || -[State!]-> I64, put!: || -[State!]-> {} }
                                                              ^^
compilation failed with 12 error(s)
"""
stderr = ""
exit = 1
