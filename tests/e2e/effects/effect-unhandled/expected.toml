stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-unhandled/src/Main.camp

I expected `identifier` here, but I got `-` instead.
   1 | Ask! : { read!: || -[Ask!]-> I64 }
                          ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-unhandled/src/Main.camp

I expected `:` here, but I got `[` instead.
   1 | Ask! : { read!: || -[Ask!]-> I64 }
                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-unhandled/src/Main.camp

I expected `identifier` here, but I got `]` instead.
   1 | Ask! : { read!: || -[Ask!]-> I64 }
                                ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-unhandled/src/Main.camp

I expected `:` here, but I got `->` instead.
   1 | Ask! : { read!: || -[Ask!]-> I64 }
                                 ^^
compilation failed with 4 error(s)
"""
stderr = ""
exit = 1
