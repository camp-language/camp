stdout = """
canonicalized /tmp/camp-e2e/effects/effect-handler-resume-twice/src/Main.camp: 2 declaration(s), 0 import(s)
typecheck passed for /tmp/camp-e2e/effects/effect-handler-resume-twice/src/Main.camp
compiled /tmp/camp-e2e/effects/effect-handler-resume-twice/src/Main.camp -> /tmp/camp-e2e/effects/effect-handler-resume-twice/src/Main.wasm
"""
stderr = ""
exit = 0
wasm_exit = 0
wasm_stdout = ""
wasm_stderr = ""
# skip_wasm: user-defined handle/resume dispatch traps at runtime
# (`undefined element: out of bounds table access` in the resume continuation).
# The earlier `0x706d6162` memory fault was the RC double-free in
# src/ir/rc.odin (emit-drop for a fully-consumed heap let-binding) — fixed.
# The remaining trap is the IR_Resume evidence/env not being threaded into
# the handler (env local holds 0, so the resume fn_idx is read from address
# 0+8). Tracked in camp-esbs.
skip_wasm = true
