stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `:` here, but I got `}` instead.
   1 | effect IO { println }
                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `:` here, but I got `=` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I was expecting a type here, but I found `|` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
               ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `:` here, but I got `|` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I was expecting a type here, but I found `->` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                  ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `:` here, but I got `{` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                     ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `=` here, but I got `{` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                              ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `in` here, but I got `.` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `.` here, but I got `IO` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                               ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `identifier` here, but I got `.` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `!` here, but I got `println` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                  ^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `=>` here, but I got `->` instead.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                             ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I was not expecting `)` here.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                                        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I was not expecting `}` here.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I was not expecting `}` here.
   2 | main! = || -> {IO} I64 { handle IO.println("hi") with { IO.println(s) -> resume(()) } 0 }
                                                                                               ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `)` here, but I got `` instead.
   3 | 
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-deep-handler/effect-deep-handler.camp

I expected `}` here, but I got `` instead.
   3 | 
       ^
compilation failed with 17 error(s)
"""
stderr = ""
exit = 1
