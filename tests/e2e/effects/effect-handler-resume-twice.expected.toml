stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `}` instead.
   1 | effect IO { println }
                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `handle` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                ^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `println` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                          ^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `"hi"` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                  ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `with` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                        ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `IO` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                               ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `.` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `s` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                          ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `->` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                             ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                 ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `(` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                  ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `identifier` here, but I got `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `:` here, but I got `)` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                    ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I was expecting a type here, but I found `}` instead.
   2 | result = handle IO.println("hi") with { IO.println(s) -> resume(()) resume(()) }
                                                                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/effects/effect-handler-resume-twice/effect-handler-resume-twice.camp

I expected `}` here, but I got `` instead.
   3 | 
       ^
compilation failed with 26 error(s)
"""
stderr = ""
exit = 1
