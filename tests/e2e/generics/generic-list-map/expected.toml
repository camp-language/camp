stdout = """
-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `/` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `The` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
               ^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `(` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
                                    ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `exit` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
                                              ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `)` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
                                                          ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `file` instead.
   1 | // BUG: The compiler crashes (SIGSEGV, exit code 11) on this file.
                                                                    ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `/` instead.
   2 | // Root cause: the recursive generic `map` function references itself by name.
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `recursive` instead.
   2 | // Root cause: the recursive generic `map` function references itself by name.
                          ^^^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `function` instead.
   2 | // Root cause: the recursive generic `map` function references itself by name.
                                                  ^^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `by` instead.
   2 | // Root cause: the recursive generic `map` function references itself by name.
                                                                             ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `/` instead.
   3 | // During monomorphization or type inference, the compiler enters infinite
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `or` instead.
   3 | // During monomorphization or type inference, the compiler enters infinite
                                  ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `,` instead.
   3 | // During monomorphization or type inference, the compiler enters infinite
                                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `enters` instead.
   3 | // During monomorphization or type inference, the compiler enters infinite
                                                                  ^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I was not expecting `/` here.
   4 | // recursion trying to resolve the recursive call, overflowing the stack.
        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `trying` instead.
   4 | // recursion trying to resolve the recursive call, overflowing the stack.
                    ^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `the` instead.
   4 | // recursion trying to resolve the recursive call, overflowing the stack.
                                      ^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `,` instead.
   4 | // recursion trying to resolve the recursive call, overflowing the stack.
                                                        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `stack` instead.
   4 | // recursion trying to resolve the recursive call, overflowing the stack.
                                                                          ^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `/` instead.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
       ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `add` instead.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
               ^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `support` instead.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
                                      ^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `)` here, but I got `in` instead.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
                                                              ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `monomorphization` instead.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
                                                                         ^^^^^^^^^^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I was not expecting `)` here.
   5 | // Fix: add recursive function support (self-reference in generic monomorphization)
                                                                                         ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I was not expecting `/` here.
   6 | // or require explicit type annotations for recursive generic functions.
        ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `type` instead.
   6 | // or require explicit type annotations for recursive generic functions.
                              ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `recursive` instead.
   6 | // or require explicit type annotations for recursive generic functions.
                                                   ^^^^^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `.` instead.
   6 | // or require explicit type annotations for recursive generic functions.
                                                                              ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `|` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
             ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `xs` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                 ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `{` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                     ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `match` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                       ^^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `{` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `identifier` here, but I got `=>` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                      ^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `Cons` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                             ^^^^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `)` here, but I got `,` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                      ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `(` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                           ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `t` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                               ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I was not expecting `)` here.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                                ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I expected `=` here, but I got `}` instead.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                                   ^

-- SYNTAX ERROR --- /tmp/camp-e2e/generics/generic-list-map/src/Main.camp

I was not expecting `}` here.
   7 | map = |f, xs| { match xs { Nil => Nil | Cons(h, t) => Cons(f(h), map(f, t)) } }
                                                                                     ^
compilation failed with 42 error(s)
"""
stderr = ""
exit = 1
