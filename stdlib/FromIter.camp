// FromIter trait — construct a collection from an iterator
// e is the effect row of the iterator's next operation.
// For pure collections, e = [].

FromIter(a, e) : {
  from_iter : |Iter(a, e)| -[e]-> Self,
}