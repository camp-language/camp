// TryFrom trait — fallible type conversion
// Self = source type (params[0]). Return is Result(target, error).
// Multi-param: same source can have TryFrom impls for many targets.
// Injected by the prelude; this file is documentation only.

TryFrom : {
  try_from : |Self| -> Result(target, error),
}

